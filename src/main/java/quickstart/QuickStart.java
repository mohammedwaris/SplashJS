/*package quickstart;

import static def.dom.Globals.alert;
import static def.jquery.Globals.$;

import java.util.ArrayList;
import java.util.List;

import flashjs.application.*;
import flashjs.display.*;
import flashjs.events.*;

import def.js.Array;

/**
 * This class is used within the webapp/index.html file.
 */
 
 /*
public class QuickStart {

	public static void main(String[] args) {
		
		
		UhudApplication app = new UhudApplication();
		Image image = new Image("fish.svg");
		image.setWidth(100);
		image.setHeight(100);
		image.setX(200);
		image.setY(200);
		
		System.out.println(app.getStage());
		app.getStage().addChild(image);
		app.getStage().addEventListener(Event.ENTER_FRAME, new EventHandler() {
			public void handle(Event event) {
				image.setRotation(image.getRotation() + 2);
				//image.setAlpha(image.getAlpha() - 0.01);
			}
		});
		app.start();
		
		
		// you can use regular Java API
		List<String> l = new ArrayList<>();
		l.add("Hello");
		l.add("world");
		// and you can also use regular JavaScript APIs
		Array<String> a = new Array<>();
		a.push("Hello", "world");
		// use of jQuery with the jQuery candy
		//$("#target").text(l.toString());
		// use of the JavaScript DOM API
		//alert(a.toString());
	}

}*/
